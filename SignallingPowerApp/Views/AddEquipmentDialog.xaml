<Window x:Class="SignallingPowerApp.Views.AddEquipmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Add Equipment" 
        Height="250" 
        Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <DockPanel>
        <!-- Title Bar -->
        <Border DockPanel.Dock="Top" 
                Background="#FFE0E0E0" 
                BorderBrush="Gray" 
                BorderThickness="0,0,0,1"
                Padding="10">
            <TextBlock Text="Select Equipment Type" 
                       FontWeight="Bold" 
                       FontSize="16"/>
        </Border>
        
        <!-- Button Bar -->
        <Border DockPanel.Dock="Bottom" 
                Background="#FFF5F5F5" 
                BorderBrush="Gray" 
                BorderThickness="0,1,0,0"
                Padding="10">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button x:Name="OkButton" 
                        Content="OK" 
                        Width="80" 
                        Height="30" 
                        Margin="5,0"
                        Click="OkButton_Click"
                        IsDefault="True"/>
                <Button x:Name="CancelButton" 
                        Content="Cancel" 
                        Width="80" 
                        Height="30" 
                        Margin="5,0"
                        Click="CancelButton_Click"
                        IsCancel="True"/>
            </StackPanel>
        </Border>
        
        <!-- Content Area -->
        <StackPanel Margin="20">
            <TextBlock Text="Select the type of equipment you want to add:" 
                       Margin="0,0,0,15"
                       TextWrapping="Wrap"/>
            
            <ComboBox x:Name="EquipmentTypeComboBox" 
                      Height="30"
                      Padding="5"
                      FontSize="14">
                <ComboBoxItem Content="Conductor" IsSelected="True"/>
                <ComboBoxItem Content="Transformer/UPS"/>
                <ComboBoxItem Content="Alternator"/>
                <ComboBoxItem Content="Consumer"/>
            </ComboBox>
        </StackPanel>
    </DockPanel>
</Window>
